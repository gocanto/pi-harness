{
  "name": "workflows",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p .",
    "test": "vitest run --config ../../vitest.config.ts ../../extensions/workflows/*.test.ts"
  },
  "devDependencies": {
    "@effect/tsgo": "^0.24.2",
    "typescript": "^7.0.2"
  }
}
