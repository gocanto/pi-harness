{
  "name": "workflows",
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p .",
    "test": "vitest run --config ../../vitest.config.ts ../../extensions/workflows/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
